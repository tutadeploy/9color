# 派单系统设计方案

## 📋 **需求概述**

### 核心需求
1. **冷却期机制**: 用户抢单后进入冷却期，不立即派单
2. **自动/手动派单开关**: 支持自动派单和手动派单两种模式
3. **手动派单界面**: 提供商品搜索和选择功能
4. **自动结算逻辑**: 根据派单模式决定结算方式

---

## 🎯 **一、交易订单列表改造**

### 1.1 列结构调整
- **删除**: 商品名称列
- **新增**: 自动派单列
- **修改**: 操作列（新增删除订单按钮）

### 1.2 自动派单列设计

#### **自动派单开启状态**
```
自动派单: [关闭] 按钮
```
- 默认状态为开启
- 显示一个"关闭"按钮
- 抢单时立即生成完整订单，冷却期后自动付款结算
- 冷却期内可显示"手动结算"按钮提前结算

#### **自动派单关闭状态（手动派单模式）**
```
自动派单: [开启] [匹配订单] 按钮
```
- 显示"开启"和"匹配订单"两个按钮
- 点击"开启": 隐藏"匹配订单"按钮，恢复自动派单模式
- 点击"匹配订单": 打开手动派单页面进行强制付款和派单

### 1.3 操作列扩展
- **改造功能**: 
  - **手动结算**: 适用于自动派单冷却期提前结算和手动派单完成结算
  - **取消订单**: 保留功能，根据付款状态智能处理余额回退
- **新增功能**: 删除订单按钮（包含余额回退逻辑）
- **按钮显示逻辑**:
  - 自动派单冷却中：显示"手动结算"按钮
  - 手动派单已付款：显示"手动结算"按钮
  - 所有未完成订单：显示"取消订单"和"删除订单"按钮

---

## 🛠️ **二、手动派单页面设计**

### 2.1 页面布局
```
┌─────────────────────────────────────┐
│ 用户信息区域                          │
│ 手机号: 138****8888                  │
│ 用户余额: ¥1,250.00                  │
├─────────────────────────────────────┤
│ 当前订单信息                          │
│ 商品标题: [显示当前订单商品标题]        │
│ 商品价格: ¥XXX.XX                    │
│ 商品数量: X件                        │
├─────────────────────────────────────┤
│ 商品搜索区域                          │
│ 商品标题: [搜索框]                    │
│ 价格区间: [最小价格] - [最大价格]      │
│                           [搜索] 按钮  │
├─────────────────────────────────────┤
│ 商品列表区域                          │
│ ┌─────┬──────────┬─────┬─────┐      │
│ │图片 │ 商品标题   │价格 │操作 │      │
│ ├─────┼──────────┼─────┼─────┤      │
│ │[图] │ 商品A     │¥100 │选择 │      │
│ │[图] │ 商品B     │¥200 │选择 │      │
│ └─────┴──────────┴─────┴─────┘      │
├─────────────────────────────────────┤
│              [确定派单] 按钮           │
└─────────────────────────────────────┘
```

### 2.2 功能说明
- **用户信息**: 显示手机号和当前余额
- **订单信息**: 显示当前订单的商品信息
- **搜索功能**: 支持按标题和价格区间搜索
- **商品列表**: 分页显示可选商品
- **确定派单**: 选择商品后确认派单

---

## 💾 **三、数据库设计**

### 3.1 系统配置表扩展
```sql
-- xy_system_config 表新增字段
ALTER TABLE xy_system_config ADD COLUMN auto_dispatch_enabled TINYINT(1) DEFAULT 1 COMMENT '全局自动派单开关';
ALTER TABLE xy_system_config ADD COLUMN cooling_period_minutes INT DEFAULT 1 COMMENT '冷却期时长(分钟，测试阶段默认1分钟)';
```

### 3.2 订单表扩展
```sql
-- xy_convey 表新增字段
ALTER TABLE xy_convey ADD COLUMN auto_dispatch TINYINT(1) DEFAULT NULL COMMENT '订单自动派单开关(NULL=使用用户默认设置)';
ALTER TABLE xy_convey ADD COLUMN cooling_end_time INT DEFAULT 0 COMMENT '冷却期结束时间';
ALTER TABLE xy_convey ADD COLUMN dispatch_status TINYINT(1) DEFAULT 0 COMMENT '派单状态: 0=冷却中, 1=已派单, 2=手动派单';
ALTER TABLE xy_convey ADD COLUMN manual_dispatch TINYINT(1) DEFAULT 0 COMMENT '是否手动派单';
```

### 3.3 用户表扩展
```sql
-- xy_users 表新增字段
ALTER TABLE xy_users ADD COLUMN default_auto_dispatch TINYINT(1) DEFAULT 1 COMMENT '用户默认自动派单设置';
```

### 3.4 订单状态重新设计
```
订单主状态 status（重新定义）:
- status = 0: 待付款/待结算（等待付款或等待结算）
- status = 1: 交易完成（已结算）
- status = 2: 用户取消（保留兼容性，实际不会出现）
- status = 3: 强制结算完成（保留兼容性）
- status = 4: 强制取消
- status = 5: 订单异常/冻结

派单状态 dispatch_status:
- 0: 等待状态（自动派单=冷却中，手动派单=等待匹配）
- 1: 已自动派单并结算
- 2: 已手动派单（等待手动结算）

结算状态判断逻辑:
- auto_dispatch = 1 且 dispatch_status = 0: 显示"冷却中（未付款）"
- manual_dispatch = 1 且 dispatch_status = 0: 显示"等待匹配订单"
- dispatch_status = 1: 显示"已完成（自动）"
- dispatch_status = 2 且 status = 0: 显示"等待手动结算"
- dispatch_status = 2 且 status = 1: 显示"已完成（手动）"
```

---

## 🔄 **四、业务流程设计**

### 4.1 用户抢单流程（重新设计）

#### **自动派单模式**
```
用户点击抢单
    ↓
根据用户余额立即生成订单（包含商品信息）
    ↓
设置 dispatch_status = 0 (冷却中，未付款)
    ↓
设置 cooling_end_time = 当前时间 + 冷却期
    ↓
设置 status = 0 (待付款状态)
    ↓
返回"抢单成功，订单已生成，冷却期后自动付款结算"
```

#### **手动派单模式**
```
用户点击抢单
    ↓
创建订单记录（不包含具体商品信息）
    ↓
设置 dispatch_status = 0 (等待手动派单)
    ↓
无冷却期设置
    ↓
设置 status = 0 (等待派单状态)
    ↓
返回"抢单成功，等待管理员匹配订单"
```

### 4.2 自动派单流程（冷却期后自动付款结算）
```
定时任务检查冷却期结束的订单
    ↓
筛选自动派单模式的订单 (auto_dispatch = 1 且 cooling_end_time <= 当前时间)
    ↓
检查订单是否已包含商品信息（自动派单在抢单时已生成）
    ↓
执行自动付款：
- 扣除用户余额：用户余额 -= 订单金额
- 记录付款日志（类型：自动付款）
    ↓
立即执行自动结算：
- 返还本金和佣金：用户余额 += (订单金额 + 佣金)
- 设置 status = 1 (交易完成)
- 记录结算日志（类型：自动结算）
    ↓
设置 dispatch_status = 1 (已自动派单并结算)
    ↓
发放上级奖励和团队佣金
```

### 4.3 自动派单冷却期手动结算
```
管理员在冷却期内点击"手动结算"
    ↓
检查订单状态：auto_dispatch = 1 且 dispatch_status = 0
    ↓
执行手动付款：
- 扣除用户余额：用户余额 -= 订单金额
- 记录付款日志（类型：手动付款）
    ↓
立即执行手动结算：
- 返还本金和佣金：用户余额 += (订单金额 + 佣金)
- 设置 status = 1 (交易完成)
- 记录结算日志（类型：手动结算）
    ↓
设置 dispatch_status = 1 (已手动结算)
    ↓
发放上级奖励和团队佣金
```

### 4.4 手动派单流程（强制付款模式）
```
管理员点击"匹配订单"
    ↓
打开手动派单页面
    ↓
显示用户信息和当前余额
    ↓
搜索并选择合适商品
    ↓
点击"确定派单"
    ↓
更新订单商品信息（goods_id, goods_count, num, commission）
    ↓
执行强制付款：
- 扣除用户余额：用户余额 -= 订单金额
- 记录付款日志（类型：强制付款）
- 注意：可能导致用户余额为负数
    ↓
设置 dispatch_status = 2 (已手动派单)
    ↓
设置 manual_dispatch = 1
    ↓
订单保持 status = 0 (等待手动结算)
    ↓
在订单列表显示"已派单，等待手动结算"
```

### 4.5 手动结算流程（适用于手动派单）
```
管理员点击"手动结算"（针对手动派单的订单）
    ↓
检查订单状态：dispatch_status = 2 且 status = 0
    ↓
执行手动结算：
- 返还本金和佣金：用户余额 += (订单金额 + 佣金)
- 设置 status = 1 (交易完成)
- 记录结算日志（类型：手动结算）
    ↓
发放上级奖励和团队佣金
    ↓
发送系统通知给用户
```

### 4.6 取消订单流程（改造）
```
管理员点击"取消订单"
    ↓
检查订单状态和付款情况
    ↓
IF (dispatch_status = 0 且 auto_dispatch = 1) {
    // 自动派单冷却中，未付款，直接取消
    无需余额回退
} ELSE IF (dispatch_status = 0 且 manual_dispatch = 1) {
    // 手动派单等待中，未付款，直接取消
    无需余额回退
} ELSE IF (dispatch_status > 0) {
    // 已付款的订单，需要回退
    回退订单金额到用户余额
    记录余额回退日志
}
    ↓
设置 status = 4 (强制取消)
    ↓
发送系统通知给用户
```

### 4.7 删除订单流程
```
管理员点击"删除订单"
    ↓
系统确认提示："确定删除此订单吗？余额将自动回退"
    ↓
检查订单状态和金额
    ↓
IF (订单已扣除用户余额) {
    计算回退金额 = 实际扣除金额
    用户余额 += 回退金额
    记录余额变动日志（类型：订单删除回退）
}
    ↓
删除订单记录（物理删除或逻辑删除）
    ↓
返回操作成功提示
```

---

## 💰 **五、结算逻辑设计**

### 5.1 自动派单付款结算逻辑
```
自动派单模式（抢单时生成订单，冷却期后付款结算）:
1. 抢单时：立即生成完整订单（包含商品信息），但不付款
2. 冷却期后自动执行：
   - 付款：用户余额 -= 订单金额
   - 结算：用户余额 += (订单金额 + 佣金)
   - 实际效果：用户余额 += 佣金
3. 记录付款和结算日志
4. 设置 status = 1 (交易完成)
5. 发放团队奖励
```

### 5.2 自动派单冷却期手动结算
```
管理员在冷却期内手动结算自动派单订单:
1. 检查：auto_dispatch = 1 且 dispatch_status = 0
2. 立即执行付款结算：
   - 付款：用户余额 -= 订单金额
   - 结算：用户余额 += (订单金额 + 佣金)
   - 实际效果：用户余额 += 佣金
3. 设置 dispatch_status = 1, status = 1
4. 发放团队奖励
```

### 5.3 手动派单强制付款逻辑
```
手动派单模式（强制付款）:
1. 抢单时：创建订单（不包含商品信息），不付款
2. 管理员匹配订单时：
   - 选择商品，更新订单信息
   - 强制付款：用户余额 -= 订单金额（可能为负）
   - 记录强制付款日志
3. 订单状态保持 status = 0，等待手动结算
```

### 5.4 手动派单手动结算逻辑
```
手动派单的手动结算:
1. 检查：dispatch_status = 2 且 status = 0（已付款，等待结算）
2. 执行结算：
   - 结算：用户余额 += (订单金额 + 佣金)
   - 记录结算日志
3. 设置 status = 1 (交易完成)
4. 发放团队奖励
```

### 5.5 删除订单余额处理
```
删除订单时的余额回退逻辑:
1. 检查订单状态是否已扣除用户余额
2. 如果已扣除，则将订单金额退回用户余额
3. 记录余额变动日志，标注为"订单删除回退"
4. 确保余额变动的可追溯性
```

**余额回退条件**:
- 订单状态为 `status = 0` (待派单/待结算) 且已扣除余额
- 订单状态为 `status = 5` (订单异常) 且已扣除余额
- 不论是冷却中、已派单还是等待结算，只要扣除了余额都需要回退

**不回退条件**:
- 订单状态为 `status = 1` (交易完成) - 已完成交易并结算
- 订单状态为 `status = 3` (强制结算完成) - 已强制结算
- 订单状态为 `status = 4` (强制取消) - 已处理过的取消

**特殊处理**:
- 手动派单导致用户余额为负数的情况，删除时需要将负余额归零

---

## 🔧 **六、技术实现要点**

### 6.1 前端实现
- **订单列表**: 使用Ajax实现开关切换
- **手动派单页面**: 模态框或新页面实现
- **商品搜索**: 实时搜索，分页加载

### 6.2 后端实现
- **定时任务**: 处理冷却期结束的自动派单
- **API接口**: 提供开关切换、商品搜索、手动派单、删除订单接口
- **权限控制**: 确保只有管理员可以手动派单和删除订单
- **余额处理**: 删除订单时的余额回退和日志记录

### 6.3 关键文件修改点
- `application/admin/view/deal/order_list.html` - 订单列表页面
- `application/admin/controller/Deal.php` - 订单控制器
- `application/admin/model/Convey.php` - 订单模型
- `application/admin/view/users/index.html` - 会员列表页面
- `application/admin/controller/Users.php` - 用户控制器
- 新增手动派单相关页面和控制器

---

## ❓ **七、需求确认结果**

1. **冷却期时长**: ✅ 测试阶段默认1分钟
2. **商品搜索**: ✅ 只需要标题和价格区间搜索
3. **权限控制**: ✅ 管理员和超级管理员权限即可
4. **通知机制**: ✅ 不需要通知功能
5. **批量操作**: ✅ 不需要批量操作功能

## 🆕 **八、会员列表派单控制**

### 8.1 功能需求
- 在会员列表添加"自动派单"列
- 可以控制单个会员的默认派单模式
- **区别于交易订单**: 这里只设置用户默认派单偏好，不进行实际匹配订单
- **实际匹配**: 仍在交易订单列表中进行具体操作

### 8.2 会员列表UI设计
```
会员列表新增列:
┌─────┬─────┬─────┬─────────┬─────┐
│用户 │余额 │状态 │自动派单  │操作 │
├─────┼─────┼─────┼─────────┼─────┤
│张三 │1000 │正常 │[关闭]   │编辑 │
│李四 │500  │正常 │[开启]   │编辑 │
└─────┴─────┴─────┴─────────┴─────┘
```

### 8.3 功能说明
- **开启状态**: 显示"[关闭]"按钮，点击可关闭该用户的自动派单
- **关闭状态**: 显示"[开启]"按钮，点击可开启该用户的自动派单
- **默认值**: 新用户注册时默认开启自动派单
- **优先级**: 会员设置作为默认值，交易订单中可单独覆盖

### 8.4 数据库设计
```sql
-- xy_users 表新增字段
ALTER TABLE xy_users ADD COLUMN default_auto_dispatch TINYINT(1) DEFAULT 1 COMMENT '用户默认自动派单设置';
```

### 8.5 业务逻辑
```
用户抢单时的派单模式判断:
1. 优先使用交易订单中的 auto_dispatch 设置
2. 如果交易订单中未特别设置，则使用用户的 default_auto_dispatch 设置
3. 管理员可以在交易订单列表中单独调整每个订单的派单模式
```

### 8.6 实现要点
- **会员列表页面**: `application/admin/view/users/index.html`
- **用户控制器**: `application/admin/controller/Users.php`
- **Ajax接口**: 提供切换用户默认派单模式的接口
- **权限验证**: 确保只有管理员可以修改用户派单设置

---

## 📝 **九、实现优先级**

### Phase 1 - 核心功能
1. 数据库表结构调整（订单表、用户表、配置表）
2. 会员列表UI改造（添加自动派单列）
3. 订单列表UI改造（删除商品名称列，添加自动派单列和删除按钮）

### Phase 2 - 手动派单
1. 手动派单页面开发
2. 商品搜索功能
3. 派单确认流程
4. 删除订单功能（含余额回退）

### Phase 3 - 自动化
1. 定时任务开发
2. 自动结算逻辑
3. 通知机制

### Phase 4 - 优化
1. 性能优化
2. 用户体验优化
3. 错误处理完善

---

---

## 🔄 **十、核心改造对比**

### 10.1 核心业务逻辑改造
| 改造前 | 改造后 |
|--------|--------|
| 抢单 → 生成订单 → 用户手动提交 → 扣除余额 | **自动派单**: 抢单 → 生成完整订单 → 冷却期 → 自动付款结算 |
| 用户可能余额不足无法提交 | **手动派单**: 抢单 → 等待匹配 → 强制付款 → 手动结算 |

### 10.2 付款时机根本性改造
| 模式 | 付款时机 | 结算时机 |
|------|----------|----------|
| **自动派单** | 冷却期后自动付款 | 付款后立即自动结算 |
| **手动派单** | 匹配订单时强制付款 | 管理员手动结算 |

### 10.3 强制付款功能重新定位
| 改造前 | 改造后 |
|--------|--------|
| 强制付款：强制扣除用户余额 | 强制付款：仅用于手动派单匹配时 |
| 适用于用户不主动付款的情况 | 适用于手动派单可能导致余额不足的情况 |

### 10.4 冷却期机制引入
| 自动派单 | 手动派单 |
|----------|----------|
| 有冷却期，抢单后等待自动处理 | 无冷却期，抢单后等待人工匹配 |
| 冷却期内可提前手动结算 | 匹配后立即强制付款 |

---

**设计完成时间**: 2024年12月19日  
**最后更新**: 2024年12月19日（重大逻辑调整：付款时机和冷却期机制）  
**设计师**: AI架构师  
**版本**: v3.0

